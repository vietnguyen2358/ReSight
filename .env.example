# Google Gemini — REQUIRED for Stagehand browser automation
GOOGLE_GENERATIVE_AI_API_KEY=

# OpenRouter — optional, cheaper alternative for AI SDK agent LLM calls
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-haiku-4.5

# Groq — Whisper STT (required for voice mode)
GROQ_API_KEY=

# ElevenLabs — premium TTS (optional, used if set; otherwise falls back to Deepgram)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Deepgram — Aura TTS (fallback when ElevenLabs key is not set)
DEEPGRAM_API_KEY=

# Browserbase (optional — for cloud browser mode)
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
STAGEHAND_ENV=LOCAL
